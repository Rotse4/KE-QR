name: ke_qr
description: Kenya quick responce code standard library.
version: 1.0.1
repository: https://github.com/Rotse4/KE-QR.git

environment:
  sdk: ^3.3.2

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
